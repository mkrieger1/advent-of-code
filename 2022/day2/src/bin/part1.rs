use std::error::Error;
use std::io;

use day2::rock_paper_scissors_part1;

fn main() -> Result<(), Box<dyn Error>> {
    /*
    println!("{}", rock_paper_scissors_part1(io::stdin().lock())?);
    */
    Ok(())
}
